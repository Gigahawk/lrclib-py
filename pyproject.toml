[build-system]
requires = ["setuptools<75.3", "wheel<0.44"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = ["pytest<=8.3.4"]

[project]
name = "lrclib-py"
version = "0.1.0"
description = "Stdlib only lrclib package"
readme = "README.md"
requires-python = ">=3.8,<3.9"
dependencies = []
